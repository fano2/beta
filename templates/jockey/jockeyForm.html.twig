{% extends "base.html.twig" %}
{% block head %}
<li><a href="{{ path('jockey_page') }}" class="nav-link">Nouveau</a></li> 
<li><a href="{{ path('JockeyListe') }}" class="nav-link">Liste jockey</a></li> 
{% endblock %}
{% block body %}
	<style>
		.center {
			margin: auto;
			width: 60%;
			border: 8px solid #FFF;
			padding: 10px;
		}
	</style>
	<div class="center">
		{{form_start(form, {'action': path('add_jockey'), 'method': 'POST'})}}
		{{ form(form)}}
			{{form_end(form)}}
		</div>

	{% endblock %}
