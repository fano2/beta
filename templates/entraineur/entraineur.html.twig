{% extends "base.html.twig" %}
{% block head %}
<li><a href="{{ path('entrainneur_page') }}" class="nav-link">Nouveau</a></li> 
<li><a href="{{ path('entraineurliste') }}" class="nav-link">Liste</a></li> 
{% endblock %}
{% block body %}
	<style>
		.center {
			margin: auto;
			width: 60%;
			border: 8px solid #FFF;
			padding: 10px;
		}
	</style>
	<div class="center">
		{{form_start(form, {'action': path('add_entraineur'), 'method': 'POST'})}}
		{{ form(form)}}
			{{form_end(form)}}
		</div>

	{% endblock %}
